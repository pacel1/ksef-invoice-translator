/**
 * Canonical production origin for SEO surfaces (sitemap, robots, metadata).
 * Deliberately a constant rather than NEXT_PUBLIC_APP_URL: canonical URLs
 * must not vary across deployment aliases or preview environments.
 */
export const SITE_URL = "https://tlumaczksef.pl";
